# Agents

## Cursor Cloud specific instructions

This is a greenfield repository ("calisthenics-app") with no source code, build system, or dependencies yet. The repo currently contains only a `README.md`.

- **No services to run.** There is no backend, frontend, or database configured.
- **No lint, test, or build commands** are available until a tech stack is chosen and code is added.
- **No package manager lockfile** exists; when one is added, the update script should be updated to install dependencies accordingly (e.g. `npm install`, `pip install -r requirements.txt`, etc.).

Once the tech stack is established, update this section with:
1. How to install dependencies
2. How to run the dev server(s)
3. How to run lint and tests
4. Any non-obvious caveats discovered during setup
